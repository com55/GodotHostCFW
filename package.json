{
  "name": "godot-host-cfw",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:schema": "wrangler d1 execute godot_host --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute godot_host --local --file=./schema.sql",
    "vendor": "cp node_modules/fflate/esm/browser.js public/dashboard/vendor/fflate.module.js"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "fflate": "^0.8.2",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
